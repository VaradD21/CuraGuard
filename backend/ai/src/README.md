# Legacy Prototype

This folder contains an older prototype preprocessing and feature-extraction path.

It is currently not part of the runtime used by [api/main.py](/E:/SafetyModel/api/main.py) or the production-facing modules under [model/](/E:/SafetyModel/model).

If you need to make changes to the live analyzer, work in `model/` instead.
